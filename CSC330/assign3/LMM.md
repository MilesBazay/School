#### LMM notes

You are to maintain this markdown file to keep notes on the way large
language models have helped with the completion of your A3 solution (e.g., ChatGPT, Copilot, )


##### `stamps` notes

I used the LMM to help me translate my haskell logic into rust
In particular, I asked for help understanding how to structure the same recursive approach in Rust.
I also needed help with some syntax and rust-specific details like using &[u32], and formatting the output line


##### `playground` notes

I had an error using the .pow function returning the wrong type. I asked chatgpt about it and added the as u32 to fix it. 
